# Variables públicas del frontend. No incluyas secretos ni credenciales AWS.
# Déjala vacía para conservar el adaptador simulado; usa esta URL con el stack local.
VITE_API_BASE_URL=http://localhost:8787
